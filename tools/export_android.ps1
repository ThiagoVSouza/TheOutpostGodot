#!/usr/bin/env pwsh
# STUB (milestone 1+): export an Android debug APK. Requires the Android SDK/build
# tools, Godot Android export templates, and a "Runnable" Android export preset (see
# the Android setup section of docs/initial_briefing.md).
Write-Host "export_android.ps1 is not implemented yet (needs Android SDK + templates + preset)."
exit 0
