#!/usr/bin/env pwsh
# STUB (milestone 1+): export a Windows debug build. Requires export templates for the
# exact Godot version and a configured "Windows Desktop" export preset.
Write-Host "export_windows.ps1 is not implemented yet (needs export templates + preset)."
exit 0
